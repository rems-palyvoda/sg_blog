<h2> SG Blog </h2>

<ul>
  <% @posts.each do |post| %>
    <li class="post-title"><%= link_to post.title, post_path(post) %></li>
  <% end %>
</ul>

<button>
  <%= link_to "New Post", new_post_path %>
</button>
