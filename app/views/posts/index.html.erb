<div>
  <div >
    <% @posts.each do |post| %>
      <%= link_to post_path(post.id) do %>
        <h4><%= post.title %></h4>
        <p><%= limited_posts_body(post.body) %></p>
      <% end %>
    <% end %>
  </div>
</div>